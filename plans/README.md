# Implementation Plans

Generated by the `improve` skill (deep audit) on **2026-06-17**, against commit `6c0db23`.

Each plan is self-contained: an executor with zero prior context should be able to run it
from the plan file + the repo alone. Read the plan fully before starting, honor its STOP
conditions, and update your row in the table below when done.

**Plan 001 (verification baseline) should land first** — it wires the existing-but-unrun test
suites + lint into CI. Several later plans assume those gates exist so their new tests actually run.

## Execution order & status

| Plan | Title                                                     | Priority | Effort | Risk | Depends on | Status                                |
| ---- | --------------------------------------------------------- | -------- | ------ | ---- | ---------- | ------------------------------------- |
| 001  | Wire Deno + vitest tests and lint into CI                 | P1       | M      | LOW  | —          | DONE                                  |
| 002  | Complete `.env.example`                                   | P1       | S      | LOW  | —          | DONE                                  |
| 003  | Add root `README.md` + `CLAUDE.md`                        | P2       | M      | LOW  | —          | DONE                                  |
| 004  | Fix UTC day-window timezone bug (reminders + digest)      | P1       | M      | MED  | 001        | DONE                                  |
| 005  | backfill-embeddings: LEFT JOIN RPC instead of NOT-IN      | P2       | S      | LOW  | 001        | DONE                                  |
| 006  | Standardize CORS on shared allowlist                      | P2       | S      | LOW  | 001        | DONE                                  |
| 007  | localhop parser: route through `guardedFetch`             | P2       | S      | LOW  | 001        | DONE                                  |
| 008  | scrape-source: await queue-kick when EdgeRuntime absent   | P3       | S      | LOW  | 001        | DONE                                  |
| 009  | backfill-event-enrichment: in-batch geocode + image cache | P2       | M      | LOW  | 001        | DONE                                  |
| 010  | process-notification-queue: batch push fan-out            | P2       | M      | MED  | 001        | DONE                                  |
| 011  | Nominatim distributed rate limiter                        | P3       | L      | MED  | 001        | DONE — resolved via option 3 (degrade on 429 + documented); option 1 (DB-coordinated slot) deferred unless Nominatim bans |
| 012  | cron-runner.sh dedup                                      | —        | —      | —    | —          | REJECTED — already solved (see below) |
| 013  | Pin `deno.json` supabase-js + email `@types` versions     | P3       | S      | LOW  | —          | DONE                                  |
| 014  | `event-review` deploy-config drift                        | —        | —      | —    | —          | REJECTED — not a drift (see below)    |
| 015  | Backfill rollbacks for grandfathered migrations           | P3       | L      | HIGH | —          | DONE — all 18 grandfathered migrations now ship rollbacks (batch 1: 000001, 007000, 010000, 016000; batch 2: 001000–006000, 008000, 009000, 011000, 011001, 012000–015000). `LEGACY_ALLOWLIST` empty. Full-stack DB-verified: 42-file down-stack runs clean newest-first, each down removes exactly its own objects, `db reset` → head fingerprint identical (37 tbl/212 fn/9 enum/151 idx/90 pol), 15/15 integration tests pass. Irreversible items (dropped indexes, reference-data upserts, grant-matrix/extension/publication/RLS posture in 001–006) are documented in each rollback header per the STOP rules. |
| 016  | (spike) iCal + RSS export edge function                   | P3       | M      | LOW  | —          | DONE                                  |
| 017  | (spike) Verify + document public semantic-search RPC      | P3       | S      | LOW  | —          | DONE                                  |
| 018  | (spike) Design public read REST API                       | P3       | L      | —    | —          | DONE                                  |
| 019  | Adopt Railway project into IaC management (Model A)       | P3       | L      | HIGH | —          | REJECTED — superseded by 021 (keep one project; guard, don't migrate) |
| 020  | Split crons into own IaC-managed Railway project (Model B)| P3       | L      | HIGH | —          | REJECTED — fragments deploy topology to satisfy IaC tooling; not worth it |
| 021  | Guard against missing cron env vars (no migration)        | P2       | M      | LOW  | —          | DONE                                  |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Dependency notes

- **004, 005, 006, 007, 008, 009, 010, 011** all benefit from **001** landing first: 001 makes
  the Deno/vitest tests these plans add actually run in CI. They are not hard-blocked (you can
  write tests before 001), but without 001 a regression they catch stays invisible.
- 015 is independent but HIGH risk — do it on its own, never bundled with a behavior change.

## Findings considered and rejected (do not re-audit)

- **Concurrent counter "race" in `process-tag-queue`**: not a bug. JS is single-threaded; `summary.x += 1`
  between `await`s is atomic — no lost updates possible.
- **RLS leak on `user_notification_preferences`**: not a bug. Policies are `USING (user_id = auth.uid())`
  with matching `WITH CHECK` (`migrations/20260601027000:42-55`) — correctly row-scoped.
- **`ws` WebSocket DoS via react-email (`pnpm audit` HIGH)**: react-email runs only at template
  deploy/preview time (`packages/email/scripts/deploy-templates.tsx`), never as a network-exposed
  runtime service. Not a production DoS vector. (Optional hygiene: a `pnpm.overrides` bump, not worth a plan.)
- **`.npmrc` token exposure**: `.npmrc:10` uses `${NODE_AUTH_TOKEN}` env expansion — the recommended
  pattern. The `pnpm audit` warning is generic. No secret is committed.
- **LLM responses missing `max_tokens`**: input is length-capped and JSON-schema-validated and prompts
  carry SECURITY notes (`tag-event/handler.ts`); adding `max_tokens` is a one-line hygiene tweak, not a plan.
- **`find_similar_events_by_id` "not exposed to users"**: false — already `GRANT EXECUTE ... TO authenticated, anon`
  (`migrations/20260601029000:84-85`). Reframed as plan 017 (verify/document), not "build it".
- **cron-runner.sh duplicated across 8 dirs (plan 012)**: already solved. The per-dir copies are required
  by the Docker build context (each Railway service builds with `rootDirectory: cron/<dir>`, so `COPY`
  can't reach `../_shared`). A single source lives in `cron/_shared/cron-runner.sh`, `scripts/sync-cron-runner.sh`
  propagates it, and `tests/guards/cron-runner-boundary.test.mjs:22-26` fails the build if any copy drifts.
  Nothing to do — no plan written.
- **`event-review` missing from deploy config (plan 014)**: not a drift. `event-review/` is a shared library
  (`mod.ts` + lib files, **no `index.ts`**). The deploy-cli guard derives deployable functions from dirs that
  contain `index.ts` (`tests/guards/deploy-cli-boundary.test.mjs:36`), so `event-review` is correctly excluded
  and the guard passes. Nothing to do — no plan written.

## How to verify the whole repo (current gates)

| Purpose       | Command                      | Notes                                                                |
| ------------- | ---------------------------- | -------------------------------------------------------------------- |
| Typecheck     | `pnpm run check`             | tsc on contracts + deploy-cli                                        |
| Guard tests   | `pnpm run workspace:test`    | node:test, 5 files                                                   |
| DB tests      | `pnpm run db:test`           | needs `pnpm run db:start` first (local Supabase)                     |
| Lint          | `pnpm run lint` (`oxlint .`) | wired in CI (plan 001)                                               |
| Deno fn tests | `pnpm run test:deno`         | wired in CI (plan 001)                                               |
| vitest        | `pnpm run test:functions`    | wired in CI (plan 001)                                               |

> **Local pnpm caveat**: `pnpm install`/`pnpm run`/`pnpm test` may fail locally on a transient supply-chain
> policy (`railway@3.3.2` minimum-release-age). Verify directly: `cd supabase/functions && deno test ...`,
> `cd supabase/functions && ../../node_modules/.bin/vitest run`, `./node_modules/.bin/oxlint .`. CI runs the
> pnpm scripts after a fresh `pnpm install`.
>
> **Resolved**: `supabase/functions/scrape-source/lib/process-source_test.ts` is back in `test:deno`.
> `guardedFetch` now takes an injectable `resolve` seam (default = real `resolveAndCheckPublicIp`), so the
> `sanitizeImagesForIngest` tests stub the SSRF resolver and run offline (no `--allow-net`). 359 deno tests pass.

---

## Round 2 — fresh audit (2026-06-19, against `5daa274`)

Generated by the improve skill (standard depth, 4 parallel auditors + vetting) after plans 001-021
shipped. Execute in the order below; honor each plan's STOP conditions and update its row.

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 022  | Test the notify-path functions (send-push/notify-email/send-reminders) | P2 | M   | LOW | — | DONE — merged `bda21b3` (PR #5, CIL-105) |
| 023  | Characterization tests for the queue orchestration loops               | P2 | M   | LOW | — | DONE — merged `e748f07` (PR #4, CIL-106) |
| 024  | Make `find_similar_events_by_id` wrapper SECURITY DEFINER (unblock anon)| P2 | S   | LOW | — | DONE — merged `cfc1def` (PR #1, CIL-107); +source-published gate per CodeRabbit |
| 025  | Consolidate CORS onto one module (public-vs-allowlist explicit)         | P2 | S–M | LOW | — | DONE — merged `643797f` (PR #3, CIL-108) |
| 026  | Batch the per-row DB writes/fetches in the cron pipeline                | P3 | S–M | LOW | — | DONE — merged `ff48ea4` (PR #8, CIL-109); +per-row-fallback + finally guards per CodeRabbit |
| 027  | Extract a shared Resend config module                                   | P3 | S   | LOW | — | DONE — merged `36cc73c` (PR #7, CIL-110) |
| 028  | (SPIKE) Specify the 3 unbuilt public REST API endpoints + build GET /events/{id} | P3 | M | LOW | 024 | DONE — merged `2d0a3d2` (PR #6, CIL-111) |
| 029  | (SPIKE) Choose + design per-IP rate limiting for events-api             | P3 | M   | LOW | — | DONE — merged `b065320` (PR #2, CIL-112); recommends Postgres token-bucket |

### Round 2 dependency notes

- **028 depends on 024**: the `GET /events/{id}/similar` endpoint can't serve `anon` until the
  semantic-search wrapper is SECURITY DEFINER (plan 024).
- **023 ↔ 026 coordinate**: 026 adds a prefetch in `process-tag-queue`; whichever lands second
  must keep the other green (023's scenarios must pass against 026's prefetch path).
- 022, 024, 025, 027 are independent of each other — any order.

### Round 2 — considered and rejected (do not re-audit)

- **events-feed serialization untested** — FALSE: `events-feed/index_test.ts` exists (plan 016 tested iCal/RSS escaping).
- **6 `_shared` deno tests "never run in CI"** — FALSE: they run under **vitest** (`supabase/functions/vitest.config.ts` includes `_shared/**/*.test.ts`); the `test:deno --ignore` list exists precisely to avoid double-running them.
- **Dead `npm:openai` dependency** — premise wrong: `openai` is NOT in the `supabase/functions/deno.json` import map (only a transitive `node_modules/.deno` artifact under `events-api`). Removal isn't the clean `deno.json` edit claimed; needs separate investigation of why it's pulled. Not planned.
- **process-notification-queue "silent loss"** (entry marked processed even if the in-app insert fails, `index.ts:305`) — defensible at-most-once tradeoff: email + push are dispatched in the same loop, so retrying would DUPLICATE them. The proposed "don't mark processed on insert failure" fix would introduce duplicate-email/push bugs. Not planned.
- **`railway_cron_runs` missing RLS policy / admin-run-cron audit integrity** — `private` schema (not PostgREST-exposed) + admin is already a trusted role. Non-issues.
- **DST day-window edge in send-reminders / no-throw on total digest failure** — subtle/rare; plan 004 fixed the real UTC day-window bug; a partial-success `200` with `{failed:N}` counts is a valid response (marking the whole run error would create alert noise). Low; not planned.
- **backfill-event-enrichment god-function (~680 lines)** — real size, but no churn/bug evidence motivating a MED-risk refactor of the enrichment core. Defer (matches the prior "not worth it until there's evidence" posture).
- **Validation-pattern / send-* handler-factory abstractions** — speculative premature abstraction; the per-function shapes differ enough that a forced factory adds risk. Not planned.

### Round 2 direction (planned as spikes 028 + 029)

- Build the 3 unbuilt public REST API endpoints — designed in `docs/PUBLIC_API.md` (plan 018). Spec'd by 028; `/{id}/similar` gated on 024.
- Rate-limit `events-api` before public announcement — explicit code TODO. Decided + designed by 029; implementation is a follow-up.
